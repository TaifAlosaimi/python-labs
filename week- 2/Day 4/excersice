name = input("Enter student name: ").strip()

if name == "":
    print("Invalid name!")
else:
    score = input("Enter score: ")
    if score.isdigit():
        score = int(score)
if 0 <= score <= 100:

             if score >= 90:
                grade = "A"
             elif score >= 80:
                grade = "B"
             elif score >= 60:
                grade = "D"
             else:
                grade = "F"

                
             course = input("Enter course: ").strip().lower()

             match course:
                case "python":
                    print("Course accepted")
                case "web":
                    print("Course accepted")
                case "ai":
                    print("Course accepted")
                


else:
    print("Score must be between 0 and 100")
print(f"Student: {name}")
print(f"Score: {score}")
print(f"Grade: {grade}")
